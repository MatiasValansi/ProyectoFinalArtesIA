export class User {
	constructor(id, email, password, role, createdAt) {
        this.id = id
        this.email = email
        this.password = password
        this.role = role
        this.createdAt = createdAt
    }
}
